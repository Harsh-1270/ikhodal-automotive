version: 2
updates:
  # Frontend (working)
  - package-ecosystem: "npm"
    directory: "/frontend"
    schedule:
      interval: "weekly"

  # Backend (ONLY if package.json exists)
  # Uncomment ONLY if valid
  # - package-ecosystem: "npm"
  #   directory: "/backend"
  #   schedule:
  #     interval: "weekly"